title = "better-git-review gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Public documentation placeholders"
regexTarget = "match"
regexes = [
  '''example\.com''',
]
